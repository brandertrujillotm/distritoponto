---
import { getCollection } from 'astro:content';
import Layout from '../../layouts/Layout.astro';

const obras = (await getCollection('obras')).sort(
  (a, b) => b.data.fecha.valueOf() - a.data.fecha.valueOf(),
);

const estadoStyles = {
  'en ejecución': 'bg-amber-100 text-amber-900 ring-amber-200',
  terminada: 'bg-emerald-100 text-emerald-900 ring-emerald-200',
  planificada: 'bg-sky-100 text-sky-900 ring-sky-200',
} as const;

const formatDate = (date: Date) =>
  new Intl.DateTimeFormat('es-PE', {
    day: '2-digit',
    month: 'long',
    year: 'numeric',
  }).format(date);
---

<Layout>
  <main class="min-h-screen bg-slate-50 text-slate-900">
    <section class="border-b border-slate-200 bg-[#e8f1ed]">
      <div class="mx-auto max-w-7xl px-5 py-14 sm:px-8 sm:py-18 lg:px-10 lg:py-20">
        <p class="flex items-center gap-3 text-xs font-bold uppercase tracking-[0.2em] text-emerald-800"><span class="h-px w-9 bg-emerald-700"></span>Transparencia y gestión</p>
        <h1 class="mt-5 max-w-3xl text-4xl font-black leading-tight tracking-tight text-slate-950 sm:text-6xl">Proyectos y Avance de Obras</h1>
        <p class="mt-5 max-w-2xl text-base leading-7 text-slate-600 sm:text-lg">Conoce los proyectos que transforman nuestro distrito, su estado actual y las acciones que impulsamos por el bienestar de Pontó.</p>
      </div>
    </section>

    <section class="mx-auto max-w-7xl px-5 py-12 sm:px-8 sm:py-16 lg:px-10" aria-labelledby="listado-obras">
      <div class="mb-8 flex items-end justify-between gap-4">
        <div>
          <p class="text-xs font-bold uppercase tracking-[0.2em] text-slate-500">Cartera municipal</p>
          <h2 id="listado-obras" class="mt-2 text-2xl font-black tracking-tight text-slate-950 sm:text-3xl">Obras registradas</h2>
        </div>
        <span class="hidden rounded-full bg-white px-3 py-1.5 text-sm font-semibold text-slate-500 ring-1 ring-slate-200 sm:inline-flex">{obras.length} {obras.length === 1 ? 'proyecto' : 'proyectos'}</span>
      </div>

      {obras.length > 0 ? (
        <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
          {obras.map((obra) => (
            <article class="group flex h-full flex-col overflow-hidden rounded-lg border border-slate-200 bg-white shadow-sm transition hover:-translate-y-1 hover:border-emerald-300 hover:shadow-xl hover:shadow-slate-200/70">
              <div class="relative aspect-[16/10] overflow-hidden bg-gradient-to-br from-emerald-900 via-emerald-700 to-sky-800">
                <img src={obra.data.imagen} alt={`Imagen de ${obra.data.titulo}`} class="h-full w-full object-cover transition duration-500 group-hover:scale-105" loading="lazy" />
                <div class="absolute inset-0 bg-gradient-to-t from-slate-950/45 via-transparent to-transparent"></div>
                <span class={`absolute left-4 top-4 inline-flex rounded-full px-3 py-1 text-xs font-bold capitalize ring-1 ${estadoStyles[obra.data.estado]}`}>
                  {obra.data.estado}
                </span>
              </div>
              <div class="flex flex-1 flex-col p-5 sm:p-6">
                <p class="text-xs font-semibold uppercase tracking-[0.14em] text-slate-400">Fecha de registro</p>
                <time datetime={obra.data.fecha.toISOString()} class="mt-1 text-sm font-semibold text-slate-600">{formatDate(obra.data.fecha)}</time>
                <h3 class="mt-5 text-xl font-bold leading-snug text-slate-950">{obra.data.titulo}</h3>
                <p class="mt-3 flex-1 text-sm leading-6 text-slate-600">{obra.data.descripcion}</p>
              </div>
            </article>
          ))}
        </div>
      ) : (
        <div class="rounded-lg border border-dashed border-slate-300 bg-white px-6 py-16 text-center">
          <h2 class="text-xl font-bold text-slate-900">Aún no hay obras publicadas</h2>
          <p class="mt-2 text-sm text-slate-500">Pronto encontrarás aquí los proyectos de la Municipalidad Distrital de Pontó.</p>
        </div>
      )}
    </section>

  </main>
</Layout>
